<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="match_parent" android:layout_height="match_parent"
	android:orientation="horizontal">

	<ImageView android:id="@+id/status_bar_album_art"
		android:layout_width="@dimen/status_bar_album_art"
		android:layout_gravity="center_vertical" android:layout_height="@dimen/status_bar_album_art"
		android:gravity="center" />

	<LinearLayout android:layout_width="0dp"
		android:layout_height="wrap_content" android:layout_gravity="center_vertical"
		android:layout_weight="1" android:orientation="vertical"
		android:paddingLeft="10dp">

		<TextView android:id="@+id/status_bar_track_name" style="@style/StatusBarText"
			android:textColor="@color/white" android:textSize="16sp"
			android:textStyle="bold" android:text="Track" />

		<TextView android:id="@+id/status_bar_artist_name" style="@style/StatusBarText"
			android:text="Artist" />

	</LinearLayout>
	
	<ImageButton android:id="@+id/status_bar_play" style="@style/StatusBarButton"
		android:src="@drawable/play" />

	<ImageButton android:id="@+id/status_bar_next" style="@style/StatusBarButton"
		android:src="@drawable/next" />

</LinearLayout>